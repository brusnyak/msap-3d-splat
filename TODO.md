# TODO

- [ ] Fix Hallway visibility / framing: reset camera + controls target when switching PLY models.
- [ ] Make point size scale with model scale so hallway points are not effectively too small.
- [ ] Reduce any risky rendering settings only if they affect visibility (keep performance changes as secondary for now).
- [ ] Verify visually by loading each model and checking that Hallway is centered and oriented sensibly.
- [ ] Explain GPU load causes and provide optional performance toggles (shadows, pixel ratio, preserveDrawingBuffer).
